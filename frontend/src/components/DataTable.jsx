import{Paper,Table,TableBody,TableCell,TableContainer,TableHead,TableRow,Typography}from'@mui/material';export default function DataTable({columns,rows}){return <TableContainer component={Paper}><Table><TableHead><TableRow>{columns.map(c=><TableCell key={c.key}><b>{c.label}</b></TableCell>)}</TableRow></TableHead><TableBody>{rows.length?rows.map((r,i)=><TableRow key={r.id||i}>{columns.map(c=><TableCell key={c.key}>{c.render?c.render(r):r[c.key]}</TableCell>)}</TableRow>):<TableRow><TableCell colSpan={columns.length}><Typography align="center" py={3}>No records found</Typography></TableCell></TableRow>}</TableBody></Table></TableContainer>}