import{useEffect,useState}from'react';import{Typography}from'@mui/material';import api from'../api/client';import DataTable from'../components/DataTable';export default function Finance(){const[r,setR]=useState([]);useEffect(()=>{api.get('/finance/invoices/').then(x=>setR(x.data.results||x.data))},[]);return <><Typography variant="h4" mb={2}>Fee Management</Typography><DataTable rows={r} columns={[{key:'invoice_number',label:'Invoice'},{key:'student_name',label:'Student'},{key:'total_amount',label:'Total'},{key:'paid_amount',label:'Paid'},{key:'balance',label:'Balance'},{key:'due_date',label:'Due Date'}]}/></>}