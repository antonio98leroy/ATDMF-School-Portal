import{useEffect,useState}from'react';import{Typography}from'@mui/material';import api from'../api/client';import DataTable from'../components/DataTable';export default function Staff(){const[r,setR]=useState([]);useEffect(()=>{api.get('/staff/members/').then(x=>setR(x.data.results||x.data))},[]);return <><Typography variant="h4" mb={2}>Staff Management</Typography><DataTable rows={r} columns={[{key:'employee_id',label:'Employee ID'},{key:'full_name',label:'Name'},{key:'staff_type',label:'Type'},{key:'department_name',label:'Department'},{key:'phone',label:'Phone'}]}/></>}