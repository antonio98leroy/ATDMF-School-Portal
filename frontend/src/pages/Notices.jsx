import{useEffect,useState}from'react';import{Card,CardContent,Stack,Typography}from'@mui/material';import api from'../api/client';export default function Notices(){const[r,setR]=useState([]);useEffect(()=>{api.get('/communications/notices/').then(x=>setR(x.data.results||x.data))},[]);return <><Typography variant="h4" mb={2}>Notice Board</Typography><Stack spacing={2}>{r.map(n=><Card key={n.id}><CardContent><Typography variant="h6">{n.title}</Typography><Typography>{n.body}</Typography><Typography variant="caption">Audience: {n.audience}</Typography></CardContent></Card>)}</Stack></>}