import{useState}from'react';import{Alert,Box,Button,Card,CardContent,TextField,Typography}from'@mui/material';import{useAuth}from'../context/AuthContext';export default function Login(){const{login}=useAuth();const[u,setU]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');const submit=async x=>{x.preventDefault();try{await login(u,p)}catch{setE('Invalid username or password')}};return <Box sx={{minHeight:'100vh',display:'grid',placeItems:'center',bgcolor:'#f4f7f5'}}><Card sx={{width:420}}><CardContent sx={{p:4}}><Typography variant="h4" gutterBottom>School Portal</Typography><Typography color="text.secondary" mb={3}>Annie T. Doe Memorial Foundation High School</Typography>{e&&<Alert severity="error">{e}</Alert>}<Box component="form" onSubmit={submit}><TextField fullWidth label="Username" margin="normal" value={u} onChange={x=>setU(x.target.value)}/><TextField fullWidth type="password" label="Password" margin="normal" value={p} onChange={x=>setP(x.target.value)}/><Button fullWidth variant="contained" size="large" type="submit" sx={{mt:2}}>Login</Button></Box></CardContent></Card></Box>}